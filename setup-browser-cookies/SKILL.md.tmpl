---
name: setup-browser-cookies
version: 1.0.0
description: |
  将真实浏览器（Comet、Chrome、Arc、Brave、Edge）中的 cookies
  导入无头 browse 会话。它会打开交互式选择器界面，让你选要导入的域名。
  适合在测试需要登录态的页面前使用。当用户说 “import cookies”、
  “login to the site” 或 “authenticate the browser” 时触发。
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

{{PREAMBLE}}

# Setup Browser Cookies

把真实 Chromium 浏览器中的登录态导入无头 browse 会话。

## 工作方式

1. 找到 browse 二进制
2. 运行 `cookie-import-browser`，自动检测已安装浏览器并打开选择器 UI
3. 用户在浏览器里选择要导入哪些域名的 cookies
4. cookies 被解密并载入当前 Playwright 会话

## 步骤

### 1. 找到 browse 二进制

{{BROWSE_SETUP}}

### 2. 打开 cookie 选择器

```bash
$B cookie-import-browser
```

这个命令会自动检测已安装的 Chromium 浏览器（Comet、Chrome、Arc、Brave、Edge），并在默认浏览器中打开交互式选择器 UI。你可以：
- 在已安装浏览器之间切换
- 搜索域名
- 点击 “+” 导入某个域的 cookies
- 点击垃圾桶图标移除已导入 cookies

告诉用户：**“Cookie 选择器已打开。请在浏览器中选择要导入的域名，完成后告诉我。”**

### 3. 直接导入（可选）

如果用户直接指定了域名，例如 `/setup-browser-cookies github.com`，则跳过 UI：

```bash
$B cookie-import-browser comet --domain github.com
```

如果用户指定了具体浏览器，则把 `comet` 替换成对应浏览器。

### 4. 验证

用户确认完成后：

```bash
$B cookies
```

向用户展示已导入 cookies 的摘要，例如各域名的 cookie 数量。

## 说明

- 每个浏览器首次导入时，macOS 可能弹出 Keychain 对话框，请点击 “Allow” 或 “Always Allow”
- Cookie 选择器与 browse server 共用同一端口，不会额外起进程
- UI 中只展示域名和 cookie 数量，不暴露 cookie 值
- browse 会话会在命令之间保持 cookies，因此导入后可立即生效
